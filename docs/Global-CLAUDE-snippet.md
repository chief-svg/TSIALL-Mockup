# Snippet for ~/.claude/CLAUDE.md on the laptop

Paste the block below into `~/.claude/CLAUDE.md` (create the file if it does not exist). It applies to every Claude Code session on the machine, in any folder, so the workflow is available even outside the project.

```
# Breast reconstruction boards workflow
I am a plastic surgeon in the ABPS oral board case collection period. When I paste a breast cancer patient history, produce a pre-clinic brief (summary, predicted chemo and radiation plan, at most 12 questions, exam to record as numbers, two suggested reconstruction plans with a decision rule, board note) and, on request, the sectioned consult note in my voice with add-if-discussed cues beside the assessment and plan. The full method, my voice rules, the five anchor sentences, fixed practice lines, ABPS rules, evidence numbers, and failure patterns are in ~/TSIALL-Mockup/docs/STANDING-INSTRUCTIONS.md; read it first. The note template is ~/TSIALL-Mockup/docs/Consult-Template-Final.md. Patient files go in ~/TSIALL-Mockup/patients/ (gitignored); initials only; never commit them.
```
