# Copy to .env (same folder) and paste your PocketSmith developer key.
# PocketSmith web → profile icon → Security & integrations → Manage developer keys → Create Key
POCKETSMITH_KEY=paste-your-key-here

# Optional
PORT=4180
# DEMO=1   # serve bundled fixtures instead of the live API (for UI checks without a key)
